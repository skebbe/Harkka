CREATE DATABASE harkka
    WITH 
    OWNER = postgres
    ENCODING = 'UTF8'
    LC_COLLATE = 'Finnish_Finland.1252'
    LC_CTYPE = 'Finnish_Finland.1252'
    TABLESPACE = pg_default
    CONNECTION LIMIT = -1;

CREATE TYPE gender AS ENUM ('Female', 'Male');

CREATE TABLE public."Harkka"
(
    id integer NOT NULL DEFAULT nextval('"Harkka_id_seq"'::regclass),
    fname character varying(50) COLLATE pg_catalog."default" NOT NULL,
    lname character varying(50) COLLATE pg_catalog."default" NOT NULL,
    dob date NOT NULL,
    gender gender NOT NULL,
    application text COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT "Harkka_pkey" PRIMARY KEY (id)
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public."Harkka"
    OWNER to postgres;

INSERT INTO "Harkka" (id, fname, lname, dob, gender , application )
VALUES (1, 'Testi', 'Hakija', '19800101', 'Male', 'Hakulause');

Select * from "Harkka";

CREATE USER foo WITH PASSWORD 'secret';